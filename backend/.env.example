MONGODB_URI="mongodb+srv://group2user:abc12345@cluster0.oy7mfyg.mongodb.net/?retryWrites=true&w=majority&appName=Cluster0"
DB_NAME=groupDB
PORT=3000
JWT_SECRET=change_me
JWT_ACCESS_SECRET=change_me_access
JWT_REFRESH_SECRET=change_me_refresh
ACCESS_TOKEN_TTL=15m
REFRESH_TOKEN_TTL_DAYS=30
COOKIE_SECURE=false
# Optional: if you want to restrict CORS to a specific origin
# FRONTEND_URL=http://localhost:3000

# Gmail SMTP for sending OTP
EMAIL_USER=thuanluuan@gmail.com
# Use Gmail App Password (16 chars) here, not your real account password
EMAIL_PASS=eeoj pqwo vxam kdyo
EMAIL_FROM="User Portal <thuanluuan@gmail.com>"

# Dev-only: return OTP in API responses for quick testing (NEVER enable in production)
# Set to true to include OTP in JSON responses of /auth/forgot-password endpoints
DEBUG_RETURN_OTP=false

# Cloudinary (optional) for avatar uploads accessible from any device
# Sign up at https://cloudinary.com/, get credentials from dashboard
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=
# Optional folder name for avatars
CLOUDINARY_AVATAR_FOLDER=avatars

# Avatar processing (server-side resize before upload)
# Max width/height in pixels for avatar images (maintains aspect ratio, no enlargement)
AVATAR_MAX_WIDTH=512
AVATAR_MAX_HEIGHT=512